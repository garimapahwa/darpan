{
  "name": "darpan-desktop",
  "private": true,
  "version": "0.1.0",
  "description": "Darpan floating emotion-aware companion bubble",
  "author": "Darpan",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "bundle-server": "node scripts/bundle-server.mjs",
    "bake-key": "node scripts/bake-key.mjs",
    "package:mac": "npm run bundle-server && npm run bake-key && electron-builder --mac --config electron-builder.yml"
  },
  "devDependencies": {
    "electron": "44.3.0",
    "electron-builder": "^26.15.3",
    "esbuild": "^0.28.2"
  }
}
